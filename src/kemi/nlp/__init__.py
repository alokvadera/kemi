"""Natural-language processing helpers: memory extraction, query
decomposition, topic clustering, summarisation, re-ranking, and the
graph utilities. These are used by the memory pipeline; the
``formation`` module also lives in :mod:`kemi.memory.formation` (kept
under ``memory/`` for historical reasons).
"""
