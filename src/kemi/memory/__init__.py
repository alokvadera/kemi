"""Memory subsystem: the core read/write/admin surface, models, and
helpers that support the versioning and lifecycle of stored memories.

Modules in this subpackage were previously flat files in
``src/kemi/``. The move is purely organisational; the public API
exposed by ``kemi.__init__`` is unchanged.
"""
